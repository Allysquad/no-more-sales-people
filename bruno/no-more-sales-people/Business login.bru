meta {
  name: Business login
  type: http
  seq: 2
}

post {
  url: {{baseUrl}}/api/customer/login
  body: json
  auth: none
}

headers {
  Content-Type: application/json
}

body:json {
  {
    "email": "{{businessEmail}}",
    "password": "{{businessPassword}}"
  }
}
